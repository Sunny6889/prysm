# 以太坊的角色和奖惩机制

## 验证者角色概述
以太坊使用其原生加密货币以太币 (ETH) 来保证其安全。希望参与验证区块和识别链头的节点运营商，需要在以太坊上的存款合约中存入32 ETH作为质押。他们运行验证者软件检查从点对点网络接收到的新区块的有效性，以及应用分叉选择算法来识别链头，然后收到用以太币支付的报酬。

### 验证者的主要职责
对于验证者来说有两个主要角色：
1) **区块证明（Attestation）**：检查新区块并且"证明"它们是否有效，包括对源检查点、目标检查点和头部区块的投票
2) **区块提议（Block Proposal）**：当从整个验证者池子被随机选中时，提议新的区块
3) **其他职责**：验证者有时也需要负责签名聚合以及参与同步委员会

以太坊通过精心设计的奖惩机制来激励验证者诚实行为并惩罚恶意或疏忽行为。所有验证者的奖励和惩罚都在每个epoch（时段）实施一次，核心实现逻辑在[`ProcessRewardsAndPenaltiesPrecompute`](https://github.com/OffchainLabs/prysm/blob/develop/beacon-chain/core/altair/epoch_precompute.go)函数中。这套机制确保了以太坊网络的去中心化、安全性和最终确定性。

**主网运行统计（截至2024年）：**
- 总验证者数量：约100万+
- 历史罚没事件：< 1000起
- 最长inactivity leak：2020年Medalla测试网事件
- 平均网络参与率：> 95%

### 验证者的奖励简介
在了解惩罚具体措施之前，有必要需要了解一下奖励机制，因为惩罚与奖励是相辅相成的。

总奖励由五个部分之和组成，每个部分都有一个权重，决定每个部分在总奖励中的比重。 这些部分是：
1. 来源证明投票：验证者给正确的来源检查点进行了及时投票
2. 目标证明投票：验证者给正确的目标检查点进行了及时投票
3. 头部证明投票：验证者给正确的头部区块进行了及时投票
4. 同步委员会奖励：验证者参与了同步委员会
5. 提议者奖励：验证者在正确的时隙提议了区块

这些权重加起来等于64，每个部分的权重如下所示：
- TIMELY_SOURCE_WEIGHT    uint64(14)
- TIMELY_TARGET_WEIGHT    uint64(26) // 给正确的目标检查点投票权重最大，促进尽快固化
- TIMELY_HEAD_WEIGHT      uint64(14)
- SYNC_REWARD_WEIGHT      uint64(2)
- PROPOSER_WEIGHT         uint64(8)

还增加一个额外奖励来激励快速认证, 这就是`inclusion_delay_reward`, 这个值等于 `base_reward` 乘以 `1/delay`，其中 `delay` 是分隔区块提议和认证的间隔时隙数。 例如，如果在区块提议的一个时隙内提交认证，那么认证者将获得 `base_reward * 1/1 == base_reward`。 如果认证在下一个时隙到达，认证者将收到 `base_reward * 1/2`，以此类推。

通过在所提议区块中包含其他验证者的不良行为证据，区块提议者也能增加奖励。 这些奖励是鼓励验证者诚实的“胡萝卜”。 一个包含罚没的区块提议者将被奖励 `slashed_validators_effective_balance / 512`。

# 以太坊的惩罚机制详解
具体来说，以太坊的惩罚机制包括以下几个方面：
1. 检测:
网络会持续监控验证者的行为，一旦发现可疑活动，会对其进行标记，并启动审查流程。通过状态树来记录验证者的行为，例如是否参与同步委员会、是否参与证明投票，不活跃分数、slashed等。
2. 惩罚:
如果确认验证者存在错误或恶意行为，例如不作为、双重签名等，网络会对其进行惩罚，没收部分或全部质押的ETH.
3. 移除:
对于情节特别严重的验证者，可能会被暂时或永久地移除出网络，失去参与共识过程的权利.

## 惩罚机制概览

| 惩罚类型 | 触发条件 | 惩罚程度 | 恢复可能性 |
|---------|---------|---------|----------|
| General Misbehavior | 错过source/target投票 | 轻微（扣除相应奖励） | 立即恢复 |
| Inactivity Penalty | 长期不活跃 | 中等（累积扣减） | 恢复活跃后逐渐恢复 |
| Slashing | 恶意行为（双签等） | 严重（强制退出+罚没） | 不可恢复 |

在以太坊 Altair 升级中，[`ProcessRewardsAndPenaltiesPrecompute`](https://github.com/OffchainLabs/prysm/blob/develop/beacon-chain/core/altair/epoch_precompute.go)函数负责处理验证者的奖励和惩罚计算。以下是关于惩罚部分的详细解析：

## General Misbehavior
对于错过 source vote 和 target vote 的行为，该验证者的账户会被扣除与其完成 source vote 和 target vote 所获得奖励相同数额的ETH，即不光不会给奖励，还会扣除与奖励金额相同的ETH
```go
// 基础计算如果正常证明应得的奖励
attDeltas, err := AttestationsDelta(beaconState, bal, vals)
if err != nil {
    return nil, errors.Wrap(err, "could not get attestation delta")
}

// AttestationsDelta里面不活跃惩罚（Inactivity Penalty）
if !val.IsPrevEpochTargetAttester || val.IsSlashed {
    n, err := math.Mul64(effectiveBalance, val.InactivityScore) // val.InactivityScore 见下面怠惰惩罚
    if err != nil {
        return &AttDelta{}, err
    }
    attDelta.InactivityPenalty = n / inactivityDenominator
}


// 计算应该扣除的金额
delta := attDeltas[i]
balances[i] = helpers.DecreaseBalanceWithVal(balances[i], delta.SourcePenalty+delta.TargetPenalty+delta.InactivityPenalty)
```

对每个验证者，从其余额中扣除三种惩罚的总和：
- 源投票惩罚（SourcePenalty）
- 目标投票惩罚（TargetPenalty）
- 不活跃惩罚（InactivityPenalty）：如果验证者没有进行目标投票或已被罚没，则会受到额外的不活跃惩罚

而对于错过头部证明vote以及没有在规定slot产生新区块来说，是没有任何惩罚的（光是错过这些奖励，就已经很肉疼了）


## Inactivity怠惰惩罚
因为最终确定性需要获得三分之二多数投票，攻击者可以用质押以太币总数的三分之一投票来阻止网络实现最终确定性。 有一种可以防御这种攻击行为的机制：怠惰惩罚，包含了不活跃分数计算和不活跃泄漏状态逻辑。
怠惰惩罚逐渐消耗其质押的以太币，使得网络能够重新获得足够的投票比例（至少2/3）来恢复链的最终确定性。

### 不活跃分数（Inactivity Score）
上面计算有效余额扣减用到的`AttestationsDelta.InactivityPenalty`，使用的不活跃分数（Inactivity Score）的更新逻辑如下：
- 如果验证者正确参与了目标attestation（target vote）且未被罚没，其不活跃分数减少1
- 如果验证者未正确参与目标attestation或被罚没，其不活跃分数增加 InactivityScoreBias （值为4）
- 如果链不处在不活跃泄漏状态，则额外减少不活跃分数 InactivityScoreRecoveryRate （值为16，但不会低于0），也就是进行逐步的恢复减少惩罚

```go
// func ProcessInactivityScores
if v.IsPrevEpochTargetAttester && !v.IsSlashed { // IsSlashed 罚没逻辑见下个部分
    // 如果验证者正确进行了目标投票，减少不活跃分数
    if v.InactivityScore > 0 {
        v.InactivityScore -= 1
    }
} else {
    // 如果验证者没有正确进行目标投票，增加不活跃分数
    v.InactivityScore, err = math.Add64(v.InactivityScore, bias)
    if err != nil {
        return nil, nil, err
    }
}

// 此外，如果链不处于不活跃泄漏状态（Inactivity Leak），则会额外减少不活跃分数，逐步恢复成0惩罚的奖励机制。
// 也就是如果处在Inactivity Leak，如果不继续投票不活跃分数会持续累计，导致不活跃惩罚越来越重。但是参考上面的逻辑如果继续投票并且不是罚没状态，不活跃分数还是会逐步减少。
if !helpers.IsInInactivityLeak(prevEpoch, finalizedEpoch) {
    score := recoveryRate
    // 防止分数低于0
    if score > v.InactivityScore {
        score = v.InactivityScore
    }
    v.InactivityScore -= score
}
```

### 不活跃泄漏状态（Inactivity Leak）
不活跃泄漏状态（Inactivity Leak）是以太坊共识层的一种紧急保护机制，当信标链长时间无法达成最终确定性（finality）时激活。这个机制的目的是通过惩罚不活跃或投票与多数验证者相反的验证者，来防止恶意攻击和网络分裂。

根据代码分析，不活跃泄漏状态的触发条件是:
```go 
func IsInInactivityLeak(prevEpoch, finalizedEpoch primitives.Epoch) bool {
	return FinalityDelay(prevEpoch, finalizedEpoch) > params.BeaconConfig().MinEpochsToInactivityPenalty
}

func FinalityDelay(prevEpoch, finalizedEpoch primitives.Epoch) primitives.Epoch {
	return prevEpoch - finalizedEpoch
}
```
当前epoch与最后一个最终确定的epoch之间的差值（称为finality delay）超过 MinEpochsToInactivityPenalty 时，系统进入不活跃泄漏状态。在主网配置中， MinEpochsToInactivityPenalty 的值为4，这意味着：
- 如果信标链连续4个epoch（约25.6分钟）无法达成最终确定性，系统将进入不活跃泄漏状态

系统退出不活跃泄漏状态的条件是：
- 当前epoch与最后一个最终确定的epoch之间的差值（finality delay）不再超过 MinEpochsToInactivityPenalty （4个epoch）

#### 当进入不活跃泄露状态对奖惩机制的影响
在不活跃泄漏状态下（ inactivityLeak = true ），验证者的证明奖励会被完全取消 ：
- 来源奖励（Source Reward） ：正常情况下，及时证明来源的验证者会获得奖励，但在不活跃泄漏状态下，即使验证者正确证明了来源，也不会获得奖励
- 目标奖励（Target Reward） ：正常情况下，及时证明目标的验证者会获得奖励，但在不活跃泄漏状态下，即使验证者正确证明了目标，也不会获得奖励
- 头部奖励（Head Reward） ：正常情况下，及时证明头部的验证者会获得奖励，但在不活跃泄漏状态下，即使验证者正确证明了头部，也不会获得奖励
相关代码逻辑：
```go
// Process source reward / penalty
if val.IsPrevEpochSourceAttester && !val.IsSlashed {
    if !inactivityLeak {  // 只有在非不活跃泄漏状态下才给奖励
        n := baseReward * srcWeight * (bal.PrevEpochAttested / increment)
        attDelta.SourceReward += n / (activeIncrement * weightDenominator)
    }
}

// Process target reward / penalty
if val.IsPrevEpochTargetAttester && !val.IsSlashed {
    if !inactivityLeak {  // 只有在非不活跃泄漏状态下才给奖励
        n := baseReward * tgtWeight * (bal.PrevEpochTargetAttested / increment)
        attDelta.TargetReward += n / (activeIncrement * weightDenominator)
    }
}

// Process head reward / penalty
if val.IsPrevEpochHeadAttester && !val.IsSlashed {
    if !inactivityLeak {  // 只有在非不活跃泄漏状态下才给奖励
        n := baseReward * headWeight * (bal.PrevEpochHeadAttested / increment)
        attDelta.HeadReward += n / (activeIncrement * weightDenominator)
    }
}
```
同时上面`ProcessInactivityScores`函数中，当处在inactivityLeak 时，不活跃分数不会进行恢复性减少，这意味着不活跃分数会持续累积，导致不活跃惩罚越来越重。但是参考上面的逻辑如果继续投票并且不是罚没状态，不活跃分数还是会逐步减少。
```go
if !helpers.IsInInactivityLeak(prevEpoch, finalizedEpoch) {
    // 如果不在不活跃泄漏状态，额外减少 recoveryRate，也就是如果处在Inactivity Leak，如果不继续投票不活跃分数会持续累计，导致不活跃惩罚越来越重
    score := recoveryRate
    if score > v.InactivityScore {
        score = v.InactivityScore
    }
    v.InactivityScore -= score
}
```

## Slashing 罚没

在以太坊信标链中，验证者罚没（Slashing）是一种惩罚机制，用于惩罚恶意或违反协议的验证者。通过分析代码，我们可以看到罚没机制的完整实现。

以太坊中有两种主要的罚没类型：

1. 提议者罚没（Proposer Slashing）
在 `ProcessProposerSlashing` 函数中，当一个验证者为同一个区块高度（slot）签署了两个不同的区块头时，会触发提议者罚没。判断条件包括：
    - 两个区块头的 slot 必须相同
    - 两个区块头的提议者索引必须相同
    - 两个区块头必须不同
    - 验证者必须是可罚没的（未被罚没且处于活跃状态）
    - 两个区块头的签名必须有效

2. 验证者罚没（Attester Slashing）
在 `ProcessAttesterSlashing` 函数中，当验证者违反了 Casper FFG 规则时，会触发验证者罚没。有两种违规情况：
    - **双重投票（Double Vote）**：在同一个目标周期（target epoch）中对两个不同的区块进行投票
    - **包围投票（Surround Vote）**：一个投票的源周期小于另一个投票的源周期，但目标周期大于另一个投票的目标周期，即 `s_a < s_b && t_b < t_a`

### 罚没处理流程

`IsSlashableValidator` 函数用于判断一个验证者是否可以被罚没，条件是：
- 验证者未被罚没（`!slashed`）
- 验证者已激活（`activationEpoch <= epoch`）
- 验证者尚未可提取（`epoch < withdrawableEpoch`）

当验证者被罚没时，`beacon-chain/core/validators/validator.go#SlashValidator` 函数会执行以下操作：
1. 调用 `InitiateValidatorExit` 启动验证者退出流程，主要步骤如下
   - 检查验证者是否已退出
   - 计算退出队列周期，计算验证者可以退出的最早周期，并检查退出队列是否超过了限制，如果超限则将退出周期延后一个周期。
   - 设置退出周期和可提款周期
   - 更新验证者状态，将更新后的状态保存到信标链状态中
2. 将验证者的 `Slashed` 字段设置为 `true`，会产生以下影响：
   - **无法获得常规奖励**：被罚没的验证者无法获得正常的验证者奖励，所有上面计算奖励的逻辑会被跳过。
   - **延长提款等待期**：验证者的 `WithdrawableEpoch` 会被延长至少 `EPOCHS_PER_SLASHINGS_VECTOR`（约 8192 个 epoch，约 36 天）
   - **立即的余额惩罚**：验证者会受到立即的余额惩罚，惩罚金额与其有效余额成比例， Bellatrix版本1以太币，Electra 0.0078 以太币
   - **无法参与共识**：虽然验证者仍在链上，但其投票不再被计入共识
   - **在罚没状态下额外惩罚**：如前面讨论的，在`Slashed`状态下，即便正常证明不活跃分数会持续的被累加，有效余额被持续的扣减
3. 更新验证者的 `WithdrawableEpoch`，延长其提款等待期
4. 更新 `slashings` 数组，记录被罚没的有效余额，这些记录用于计算相关性罚没，当作恶的有效余额越大惩罚系数越高，其设计目的是防止大规模协调攻击：
    - **计算公式**：`penalty = validator_balance × slashed_balance_in_period / total_balance × 3`
    - **时间窗口**：在同一个8192 epoch周期内被罚没的验证者
    - **最大惩罚**：验证者的全部有效余额
    - **设计理念**：单独作恶成本较低，协调作恶成本极高
5. 计算罚没惩罚金额，验证者的有效余额除以罚没惩罚系数，从验证者余额中扣除
6. 计算举报奖励，分配给区块提议者和举报者

## 惩罚机制总结
可以根据上述三种类型的惩罚看出以太坊的惩罚机制主要关注两个核心问题：
1. 区块的及时固化 ：
   - 惩罚不参与source/target检查点投票的验证者
   - 通过递进的Inactivity Score和Inactivity Leak机制处理长期不活跃
2. 区块的正确性 ：
   - 对恶意产块的提议者进行惩罚
   - 对错误投票的验证者进行惩罚
   - 进入罚没时**立即扣减余额**，反应时间为一个slot比较快
   - 然后直接启动验证者的退出流程，并在接下来每个epoch进行额外扣减

除了罚没时的一次立即扣减，其他惩罚余额扣减都在每个epoch结束实施一次也就是每隔6分24秒

区块提议的及时性虽然重要，但不是网络安全的核心要素，因此不需要额外的惩罚机制，有以下考虑：
- 设计哲学：激励优于惩罚
    以太坊采用了"胡萝卜而非大棒"的激励机制：
    - 机会成本惩罚 ：错过一次区块提议可能损失数百美元到数千美元的收益，这种经济损失已经足够激励验证者保持在线和正常运行
    - 正向激励 ：通过奖励机制激励验证者积极参与，而不是通过惩罚机制强制参与
    正如文档中提到的："而对于错过头部证明vote以及没有在规定slot产生新区块来说，是没有任何惩罚的（光是错过这些奖励，就已经很肉疼了）"
- 网络容错性考虑
    单点故障容忍 ：
    - 每个slot只有一个验证者被选为提议者，而且随机算法，使得预谋作恶难度极高
    - 如果该验证者因为网络问题、硬件故障或其他技术原因无法产生区块，惩罚他们是不公平的
    - 网络可以容忍偶尔的空slot，不会影响整体安全性
    避免过度惩罚 ：
    - 技术故障不应该导致经济损失
    - 区块提议失败可能由多种非恶意原因造成（网络延迟、同步问题等）


以太坊对不同行为采用不同的惩罚策略：

| 行为类型 | 惩罚情况 | 原因 |
|---------|---------|------|
| 错过source/target投票 | **有惩罚** | 影响网络最终确定性，需要2/3多数 |
| 错过头部投票 | **无惩罚** | 不影响安全性，只影响分叉选择 |
| 错过区块提议 | **无惩罚** | 单点行为，不影响网络安全 |
| 双重产块、恶意投票 | **严重惩罚，启动验证者退出流程** | 恶意行为，威胁网络安全 |


 ## 网络健康状态监控

**关键指标：**
- **参与率**：当前epoch中活跃验证者比例
- **最终性延迟**：当前epoch与最后最终确定epoch的差值
- **罚没率**：近期被罚没验证者的比例
- **不活跃分数分布**：验证者不活跃分数的统计分布

**健康阈值：**
- 参与率 > 66.7%：网络正常运行
- 最终性延迟 < 4 epochs：无需启动inactivity leak
- 罚没率 < 0.1%：网络安全状况良好


## Appex: 惩罚相关变量的存储位置
InactivityScore 和 IsSlashed 这两个变量都是记录在以太坊信标链的系统状态树中 
### InactivityScore 字段
- 存储位置 ： `BeaconState.inactivityScores[validatorIndex]`
- 数据类型 ： `[]uint64` （每个验证者对应一个分数）
- 定义位置 ：在 `BeaconState` 结构体中作为独立数组
```go
type BeaconState struct {
    // ... 其他字段
    validators                          []*ethpb.Validator  // 验证者列表
    inactivityScores                    []uint64            // 不活跃分数数组
    inactivityScoresMultiValue          *MultiValueInactivityScores
    // ... 其他字段
}
```
Prysm 专有 gRPC API ：
- 方法 : `GetValidatorPerformance`
- 返回数据 : ValidatorPerformanceResponse 中的 InactivityScores 字段

### IsSlashed 字段
- 存储位置 ： `BeaconState.validators[i].Slashed`
- 数据类型 ： `bool`
- 定义位置 ：在 `Validator` 结构体中
```go
type Validator struct {
    PublicKey                  []byte
    WithdrawalCredentials      []byte  
    EffectiveBalance           uint64
    Slashed                    bool    // IsSlashed 字段
    ActivationEligibilityEpoch primitives.Epoch
    ActivationEpoch            primitives.Epoch
    ExitEpoch                  primitives.Epoch
    WithdrawableEpoch          primitives.Epoch
}
```

查询 IsSlashed 状态
Prysm 专有 gRPC API ：
- 方法 : `GetValidator`
- 方法 : `ListValidators`
- 返回数据 : `Validator` 结构中的 Slashed 字段

### 存储机制详解状态树结构
这些数据作为信标链状态的一部分，最终都会参与状态根的计算
```go
// 状态根计算中包含这些字段
func (b *BeaconState) ComputeFieldRootsWithHasher(hasher ssz.HashWalker) error {
    // 验证者根（包含 IsSlashed）
    validatorsRoot, err := stateutil.ValidatorRegistryRoot(b.validators)
    
    // 不活跃分数根
    inactivityScoresRoot, err := stateutil.Uint64ListRootWithRegistryLimit(b.inactivityScores)
    
    // 这些根都会参与最终状态根的计算
    fieldRoots := [][]byte{
        // ... 其他字段根
        validatorsRoot[:],
        inactivityScoresRoot[:],
        // ... 其他字段根
    }
}
```



# 惩罚机制更新相关EIP
EIP-7251：将验证者的最大有效余额（MAX_EFFECTIVE_BALANCE）从当前的32 ETH提高到2048 ETH

根据EIP-7251（MaxEB）最新规范<mcreference link="https://lighthouse-blog.sigmaprime.io/maxeb-inactivity-leak.html" index="1">1</mcreference>，以太坊信标链的固化逻辑主要改进如下：

1. **触发条件优化**  
   非活跃泄漏机制触发阈值从连续4个epochs缩短至3个epochs未达成最终性，加速网络恢复响应
2. **二次惩罚算法**  
   采用非线性惩罚公式：  
   `balance -= balance × (inactivity_score²) / SCALE_FACTOR`  
   其中`inactivity_score = (未最终化epoch数 - 3) × 基础惩罚值`
3. **动态退出队列**  
   验证者余额低于16 ETH时自动进入退出队列，队列处理速率公式：  
   `exit_rate = MAX_EXITS_PER_EPOCH × √(non_finalized_epochs)`  
   实现与网络异常程度自适应的验证者退出节奏
4. **最终性双重验证**  
   恢复后持续监测2个epochs，要求：  
   ```
   (活跃验证者总余额 - 惩罚余额) / 总余额 > 66.6%
   ```
   防止瞬时波动导致二次异常

该机制通过<a href="https://eips.ethereum.org/EIPS/eip-7251">EIP-7251</a>实现，相比原有设计将网络恢复时间缩短40%，同时将大规模停机场景下的ETH损失降低25%（基于百万验证者模拟数据）<mcreference link="https://blog.sigmaprime.io/maxeb-inactivity-leak.html" index="3">3</mcreference>。